# Secret names stored in Doppler (project: commentinsight, config: dev).
# See docs/DOPPLER.md — use: ./scripts/doppler-compose.sh up -d
#
YOUTUBE_API_KEY=
OPENAI_API_KEY=
CONFIG_REPO_URL=
CONFIG_REPO_BRANCH=main
CONFIG_REPO_USERNAME=
CONFIG_REPO_TOKEN=
DOCKER_HUB_USERNAME=
DOCKER_HUB_TOKEN=
